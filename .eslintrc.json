{
  "extends": ["next/core-web-vitals"],
  "ignorePatterns": [
    "node_modules/",
    ".next/",
    "dist/",
    "public/"   // ✅ ignore your service worker + firebase compat files
  ],
  "rules": {
    // you can keep custom rules here
  }
}
